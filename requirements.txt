# VIP Python dependencies
# Install via: pip install -r requirements.txt
# Or use setup.sh for the full guided setup.

# Core ML
mlx
insightface
onnxruntime>=1.20      # Official ONNX runtime; supports Apple Silicon (CoreML EP) since 1.18
faiss-cpu
# hdbscan standalone package removed: sklearn>=1.3 ships its own HDBSCAN,
# which the code already uses as primary. The standalone hdbscan 0.8.41
# requires scikit-learn>=1.6, which has a regression in epsilon_search.
scikit-learn>=1.3,<1.5
numpy

# Secondary ML — objects, animals, scene, landmarks, species
ultralytics            # YOLOv8 OIV7 (yolov8n-oiv7.pt auto-downloads on first use)
torch>=2.2             # PyTorch with MPS backend for Apple Silicon
torchvision>=0.17      # Required by Places365 ResNet-50
open-clip-torch        # OpenCLIP (landmark CLIP fallback) + BioCLIP (species)
timm>=0.9              # GLDv2 EfficientNet-B4 landmark recogniser (primary)
huggingface_hub>=0.20  # Auto-download GLDv2 weights from HuggingFace Hub

# Geo
geopy                  # Reverse geocoding via Nominatim/OSM

# Image / RAW
rawpy
Pillow

# API
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
httpx

# Database
aiosqlite

# Dev / testing
pytest
pytest-asyncio
httpx
